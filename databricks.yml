# This is a Databricks asset bundle definition for sdp_v3_pipeline.
bundle:
  name: sdp_v3_pipeline
  uuid: aae208b3-90ab-4d51-8047-afc0d0143717

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    variables:
      catalog: sdp_cata
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    # --- INDENT ROOT_PATH INSIDE WORKSPACE SECTION ---
    workspace:
      root_path: /Workspace/Users/thakkalipk007@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: sdp_cata
      schema: prod
    permissions:
      - user_name: thakkalipk007@gmail.com
        level: CAN_MANAGE

resources:
  jobs:
    first_sdp_pipeline_job:
      name: first_sdp_pipeline
      email_notifications:
        on_failure:
          - thakkalipk007@gmail.com
      notification_settings:
        no_alert_for_skipped_runs: true
      
      schedule:
        quartz_cron_expression: "0 */5 * * * ?"
        timezone_id: "UTC"
        pause_status: UNPAUSED
        
      max_concurrent_runs: 2
      tasks:
        - task_key: pipeline-task-d5798326-14f7-459b-a3c3-68f6966e006e
          pipeline_task:
            pipeline_id: d5798326-14f7-459b-a3c3-68f6966e006e
            full_refresh: true
          max_retries: 1
          min_retry_interval_millis: 100000
        - task_key: test-pipeline-task
          pipeline_task:
            pipeline_id: e3d84465-13da-498d-aa60-a5600c6227e5
            full_refresh: true
          max_retries: 1
          min_retry_interval_millis: 100000
